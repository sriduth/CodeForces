#include <iostream>
#include <vector>

using namespace std;

int main()
{
	int T,temp,i;
	vector<int> W;
	vector<char> M;

	for(i=0;i<T;i++)
	{
		cin >> temp;
		if(temp == i+1)
			temp = -1;

		W.push_back(temp);
	}

	while(true)
	{
		if(i==0)
			
	}

	return 0;
}