#include <iostream>
#include <vector>

using namespace std;

int main()
{
	cin.sync_with_stdio(false);
	int T,temp,score = 0;
	vector<int> V;

	for(i=0;i<T;i++)
	{
		cin >> temp;
		V.push_back(temp);
	}
	while(V.size() > 2)
	{
		
	}
}