#include <iostream>

using namespace std;

int main()
{
	int length,queries;

	cin >> length >> queries;

	int seq[length];

	int ones[length];
	int mones[length];

	int a;
	for(int i=0;i<length;i++)
	{
		cin >> seq[i];
		if(seq[i] == 1){
			ones[length]++;
			
		}
		else
			mones[length]
	}
	return 0;
}